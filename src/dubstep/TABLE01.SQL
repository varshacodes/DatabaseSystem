CREATE TABLE PLAYERS(ID string, FIRSTNAME string, LASTNAME string, FIRSTSEASON int, LASTSEASON int, WEIGHT int, BIRTHDATE date);

SELECT * FROM PLAYERS WHERE WEIGHT > 200;

CREATE TABLE R (A int, B int);

SELECT A, B FROM R
  UNION
SELECT A, B FROM R;

CREATE TABLE R (A int, B int);

SELECT A, B FROM R
  UNION
SELECT A, B FROM R
  UNION
SELECT A, B FROM R;

CREATE TABLE R (A int, B int);
CREATE TABLE S (B int, C int);

(SELECT B FROM R)  UNION (SELECT B FROM S);